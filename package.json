{
  "name": "agentic-dev-team",
  "private": true,
  "type": "module",
  "description": "Self-prompting AI dev-team framework — tests cover the safety-gate hooks, install scripts, and config integrity.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test 'tests/*.test.mjs'"
  }
}
